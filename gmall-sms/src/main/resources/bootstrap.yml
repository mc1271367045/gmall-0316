spring:
  application:
    name: sms-service
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: 42ca6235-1306-417d-8ffe-4f968263b957
        group: dev
        file-extension: yml  # 默认找服务名.properties文件